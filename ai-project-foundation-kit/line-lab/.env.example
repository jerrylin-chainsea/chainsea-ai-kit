# 複製這份成 line-lab/.env,填入自己的 LINE OA 資料就能「真送」。
# 三個都設好、並把 LINE_REAL_SEND 改成 1,Dashboard 的「推播 LINE Flex」按鈕與
# `node line-lab/sendLineAlert.js --flex --confirm` 才會真的送到 LINE。
# 注意:改完 .env 要「重新啟動 npm run dev」才會生效(伺服端只在啟動時讀一次)。
# .env 不會被 commit(見 .gitignore);沒有 LINE OA 時維持 mock 即可過關。
LINE_CHANNEL_ACCESS_TOKEN=請填入你的 channel access token
LINE_TARGET_ID=請填入 userId 或 groupId
LINE_REAL_SEND=0
