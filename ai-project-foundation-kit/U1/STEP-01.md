# U1 · STEP 01 ｜ 把專案跑起來，先看到最終成果

> **這堂完成物**：專案能跑、你知道作品長什麼樣、改過一個看得見的字、完成第一次 Git 存檔。
> **你現在在流水線**：`【專案跑起來】→ 檢查資料 → 資料合約 → AI 串接 → Dashboard → 修錯 → 驗收 → 交付`

## 1. 一鍵啟動

- Windows：雙擊 `start-m11.bat`
- macOS：雙擊 `start-m11.command`（被擋就右鍵 → 打開）

**你應該看到**：瀏覽器自動打開 <http://localhost:5180>，出現會動的海灘「海風小店」。

> 這一步不需要懂：npm、Vite、React。它們之後才會登場，今天只要「跑起來」。

## 2. 先看最終成果（30 秒，這門課的目標）

點網頁上方的「**營運異常 Dashboard**」按鈕。

**你應該看到**：一個有步驟卡的儀表板 —— 載入資料、檢查資料合約、payload 預覽、人工審核、模擬送出。
這就是你 U3 要親手完成、U4 要正式交付的作品。看過就好，先切回「海風小店」。

## 3. 認識 VS Code 三個區（不打 AI）

用 VS Code 開 `ai-project-foundation-kit` 資料夾，認出三個區：

- **Explorer（左邊檔案樹）**：所有檔案在這
- **Editor（中間）**：點檔案就在這裡編輯
- **Terminal（下面）**：跑指令的地方（找不到 → 選單 `Terminal ▸ New Terminal`）

## 4. 今天會碰的檔（其他都先忽略）

| 檔案 | 它管什麼 |
|---|---|
| `web-lab/src/data.js` | 首頁的文字內容：店名、標語、商品。**今天主要改這裡** |
| `data-lab/report.json` | 整個作品的資料來源（U2 主角，今天只要知道它存在） |
| `START-HERE.md` | 整門課的地圖 |

→ 下一步：打開 `STEP-02.md`，做你的第一次修改與第一次存檔。
