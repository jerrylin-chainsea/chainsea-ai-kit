---
title: 我的第一篇文章
date: 2026-06-17
---

這是我用 AI agent 在 IDE 裡做出來、再公開發布到網路上的測試文章。

放圖片很簡單：把圖丟到 `public/images/`，然後這樣寫（絕對路徑）：

![測試圖](/images/sample.svg)

（小提醒：上面的 `/images/...` 是「網站根目錄」路徑。若你在 `astro.config.mjs` 把 `base` 改成 `/你的repo名/`，圖片路徑前面也要加上同樣的前綴。部署到 `<帳號>.github.io` 時 base 是 `/`，直接這樣寫就好。）

之後我會把這篇換成真正想分享的內容。
