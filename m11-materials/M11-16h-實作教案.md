# M11 / U11 ｜ 16 小時 AI 程式開發實作課 · 老師劇本(教案）

> **這份是老師的「劇本」**：4 堂 × 4 小時 = 16 小時。學生**照劇本一步步做**，不自由發想。
> 每個動作都有:**固定 prompt → 固定操作 → 預期結果 → 驗收 → 出錯保底**。全班做出同一個結果,再往下一步。

---

## 最高原則(每一步都適用)

1. **可預期**:同一段 prompt、同一個操作,全班看到同一個結果。
2. **可驗收**:每步都有「怎麼確認做對了」——多半是**看畫面**或**看 `git diff`**。
3. **有保底**:任何一步卡住,都有 B 方案,不讓任何學生卡在原地(見每步「保底」與最後的〈通用保底〉)。
4. **人是主管,AI 是工讀生**:先讀 → 先計畫 → 加護欄 → 小範圍改 → 看 diff 驗收。順序對了才安全。

## 課程主線(一條線)

```
資料進來 → 檢查資料 → 資料合約 → AI coding agent 改功能 → Dashboard 呈現
       → LINE Flex 推播 → ops agent 自動化 → GitHub Actions → reviewer / build / commit
   ▲            C1 進得了專案        C2 管得住 AI           C3 接上平台              C4 自動化交付
```

整門課只做**一條交付線**:「倉儲營運 Dashboard + LINE OA Flex 推播 + ops agent 自動化」。海風小店是載體,主線是 **AI coding agent 驅動開發與平台整合**:
C1 把作品跑起來、先看到最終成果 → C2 用資料合約與 planner/implementer/reviewer 工作流管住 AI → C3 用 Codex/Claude Code 接 LINE OA Flex 並保留人工審核 → C4 用 CrewAI-style ops agent + GitHub Actions 自動產 artifact,再用 reviewer/build/Skill/MCP 收乾淨。

> **整門課 DoD(每堂重複)**:AI 做出來不算完成,通過驗收才算完成。驗收包含:**畫面／輸出／diff／build／human review**。

## 四堂總覽

| 堂 | 標題 | 目標(帶得走) | 完成物 | 對應 starter 路徑 |
|---|---|---|---|---|
| **C1** | 進得了專案 | 一鍵啟動 → 先看最終成果 → 改一個字 → 存檔 | 跑起 web-lab、看過 Dashboard 成品、改 `data.js`、第一次 `git commit` | `START-HERE.md`、`U1/`、`web-lab/` |
| **C2** | 管得住 AI | 資料合約 + planner→人審→implementer,新增低庫存推播準備區塊 | 看懂 `report.json` 合約、一次 A–F 計畫與 A–E 回報、Dashboard 小改、build/diff/commit | `U2/`、`data-lab/report.json`、`prompts/02-03` |
| **C3** | 接上平台 | Dashboard 按鈕流程:載入→檢查→Flex 預覽→人審→mock;製造合約錯→ReAct 修 | Dashboard 全流程走通、Flex payload/mock/`[blocked]`/合約錯誤親眼看過、build 通過 | `U3/`、`web-lab/src/Dashboard.jsx`、`line-lab/`、`prompts/04` |
| **C4** | 自動化交付 | CrewAI-style ops agent、GitHub Actions artifact、reviewer、Skill/MCP | ops agent 產 report、Actions 產 Flex artifact、reviewer PASS、自建 `/ops-check` `/ship-check` | `U4/`、`ops-agent-lab/`、`.github/workflows/`、`.claude/` |

> **每堂學生直接打開 starter 的 `U1/`–`U4/`**:`STEP-*.md`(照著做)、`PROMPT-CARD.md`(複製貼上)、`ACCEPTANCE.md`(驗收)、`PITFALL.md`(保底)。本教案是老師視角的控場劇本。

> **環境需求**:VS Code + Claude Code 擴充(C2 起)、Node 18+、Python 3、可上網。Codex 擴充於 C3 安裝。

---

# C1 ｜ 進得了專案(0:00–4:00)

> 目標:**先看到最終成果,再拆開工具**。今天不寫程式,但你會一鍵啟動、看到作品目標、改一個字、存檔。
> 每個工具只講「它現在幫你做什麼」,不講「它還能做什麼」。學生講義:`U1/STEP-01.md`、`U1/STEP-02.md`。

| 時間 | 動作 | 固定 prompt / 指令 | 固定操作 | 預期結果 | 驗收 | 出錯保底 |
|---|---|---|---|---|---|---|
| 0:15–0:45 | 一鍵啟動 + 成果預覽 | 雙擊 `start-m11.bat`(mac:`start-m11.command`) | 等瀏覽器自動開 → 點「營運異常 Dashboard」看最終成果 → 切回首頁 | 看到海風小店 + 知道整門課要做出的 Dashboard 長怎樣 | 說得出「這門課最後交出什麼」 | 一閃就關 → 先裝 Node LTS;沒自動開 → 手動開 `localhost:5180` |
| 0:45–1:30 | 認識 VS Code | —(不打 AI) | 開資料夾 `ai-project-foundation-kit`;認 Explorer / Editor / Terminal | 左邊出現檔案清單 | 說得出這三區在哪 | 找不到 Terminal → 選單 `Terminal ▸ New Terminal` |
| 1:30–2:10 | 認檔案角色(只看今天會碰的) | — | 點開 `START-HERE.md`、`web-lab/src/data.js`、瞄一眼 `data-lab/report.json` | 看得懂哪個檔管什麼 | 指得出 `data.js` | 檔案沒出現 → 重開資料夾選到對的一層 |
| 2:10–2:50 | 指令版啟動(理解一鍵啟動做了什麼) | `cd web-lab` → `npm install` → `npm run dev` | 在 Terminal 依序打 | 出現 `localhost:xxxx` | 知道 start-m11 幫你做的就是這三步 | 埠被占 → Vite 會自動換埠,用它給的網址;裝不動 → 見〈通用保底〉Node |
| 2:50–3:20 | 改一個字 | —(手改) | `data.js` 把 `name: '海風小店'` 改成你的店名 → 存檔 | 瀏覽器**熱更新**,招牌變你的店名 | 招牌真的變了 | 沒變 → 確認存檔(分頁圓點消失)、dev 還在跑 |
| 3:20–3:55 | 第一次存檔 | `git status` → `git diff` → `git add .` → `git commit -m "把店名改成我的店"` | 在 Terminal 依序打 | diff 只有 `data.js` 一行、出現一筆 commit | `git status` 乾淨 | 說 `not a git repository` → 先 `git init`;要 email → 照提示設定後重 commit |

**C1 收束一句**:工具不可怕,它是「專案的入口」。你已經會打開、跑、改、存 —— 而且你知道終點長什麼樣。

---

# C2 ｜ 管得住 AI(0:00–4:00)

> 目標:裝 Claude Code,認識**資料合約**(`data-lab/report.json`),學會 **planner → 人審 → implementer** 工作流;並養成內建 slash 的好習慣。
> 開場先「痛一次」:老師示範沒有護欄的 AI 亂改三個檔的 diff,再引出 AGENTS.md/CLAUDE.md 與固定流程的必要性。學生講義:`U2/STEP-01..02.md`。

## 段 1 裝上、認識面板(0:15–1:10)
- 在 VS Code 擴充功能搜 `Claude Code`(Anthropic)→ Install → 開面板 → 登入。
- **驗收**:面板能打字。**保底**:面板沒出現 → 重開 VS Code;登入轉圈 → 完成瀏覽器授權後等幾秒。

## 段 2 兩個旋鈕:模式 + 模型(1:10–2:20)
- **模式**(給 AI 多少動手權):`shift+tab` 循環 Default → Auto-Accept → **Plan**。
- **模型**:打 `/model` → 選 Opus(難題)/ Sonnet(小事)。**跑跑看**:打完 `/model` 右上「模型」標籤真的換人。
- **內建 slash 生活習慣**(本堂新增,見〈附錄 A〉):`/clear`、`/compact`、`/review`、`/status`。
- **保底**:Windows 上 `shift+tab` 有時跳過 Plan → 改用**點面板底部模式字樣**選 Plan。

## 段 3 先讀、不先改(2:20–3:05)
- **固定 prompt**:`prompts/01-read-project.md`(請先閱讀專案、不要改、回答五問)。
- **操作**:確認底部是 **Plan mode** → 貼 prompt → 送出。**驗收**:拿到摘要且 `git status` 乾淨(一個檔都沒改)。

## 段 4 planner → 人審 → implementer,安全改一次(3:05–3:55)
- **固定 prompt**:`prompts/02-plan-mode.md`(planner,只回 A–F 格式)→ 人審四題(檔案對嗎/新套件/package.json/重構傾向)→ `prompts/03-guardrails.md`(implementer,回報 A–E 格式)。
- **任務**:在 Dashboard 載入 report.json 後,新增「低庫存推播準備」小區塊。允許檔案只有 `web-lab/src/Dashboard.jsx` 與 `web-lab/src/styles.css` 的 Dashboard 區塊。
- **操作**:Plan mode 貼 planner 卡 → 看 A–F 計畫、問四題 → `shift+tab` 切離 Plan → 貼 implementer 卡 → 照它回報的 D 逐項驗收。
- **第二輪變更**:再給一個小改動,**仍走同一流程** —— 這就是「可交付」和「碰運氣」的差別。
- **驗收**:Dashboard 出現推播主題/通道/下一步、`git status` 只有允許檔案、`npm run build` 通過、`git diff` 沒動 LINE 腳本與 package.json → `git commit`。
- **保底**:AI 亂改一堆 → `git restore <file>` 還原後,加更嚴格護欄重下(見〈通用保底〉)。

**C2 收束一句**:AI 不是不能改,是**不能一開始就改**。你驗收過,才算完成。

---

# C3 ｜ 接上平台(0:00–4:00)

> 目標:在**營運異常 Dashboard** 上,以按鈕為主走完「載入 → 檢查合約 → Flex payload 預覽 → 人工審核 → mock 送出」,
> 再親手製造合約錯誤、用 **ReAct** 修好。今天先求 **mock 成功**,不求真送成功;真送是老師示範/進階組。
> 學生講義:`U3/STEP-01..03.md`。老師詳版:`u11-c3-line-oa-lab.md`。

任務素材:`web-lab/src/Dashboard.jsx`(starter 已內建)+ `data-lab/report.json`(唯一資料來源)+ `line-lab/sendLineAlert.js`。
**教學用固定錯誤**:把 `report.json` 的 `risk_level` 改成中文「嚴重」→ 畫面出現紅色擋牌、腳本送出前阻擋(兩道防線、同一句錯誤)。

## 段 1 按鈕走主線(0:15–1:20)
- 一鍵啟動 → 切「營運異常 Dashboard」→ Button 1 載入範例 → Button 2 檢查資料合約(綠色通過)→ Button 3 生成 Flex payload 預覽。
- **對照 30 秒**:`node line-lab/sendLineAlert.js --flex` 產生 `line-lab/line-flex-payload.json`;`to` 在網頁永遠是示範 ID(token 與對象不進前端)。
- **驗收**:三步卡都打勾、說得出「畫面讀的就是 report.json,沒有第二份資料」。
- **保底**:畫面跑不動 → 用終端機 `node line-lab/sendLineAlert.js --flex` + 看 payload 完成同一段驗收。

## 段 2 Human-in-the-loop + mock(1:20–2:00)
- 未勾 checkbox → 真送指令**不出現**;認真讀過通知文字 → 勾「我已人工審核通知內容」→ Button 5 模擬送出 → 看到 `[mock] LINE_REAL_SEND is not 1, no request sent.` → 真送指令出現(僅供複製)。
- **固定 prompt**:`U3/PROMPT-CARD.md` 卡 1(HITL 審核,AI 只判斷不改檔)。
- **終端機對照(僅三行)**:mock 成功、`LINE_REAL_SEND=1` 無 `--confirm` 看到 `[blocked]`。
- **驗收**:F12 Network 全程零請求、`[mock]` 與 `[blocked]` 都親眼看過。

## 休息(2:00–2:15)

## 段 3 製造合約錯 → ReAct 修好(2:15–3:20)
- 手改 `report.json`:`"risk_level": "high"` → `"嚴重"`(**改值不改語法**)→ 存檔 → 擋牌**不點擊自己出現**、下游全部消失;跑 `node line-lab/sendLineAlert.js --flex` 看到同一句錯誤。
- **固定 prompt**:`U3/PROMPT-CARD.md` 卡 2(ReAct:Expected/Actual/Reason/Act/Observe/**Minimal Patch**/Verify/Blocker,先分析不改檔)→ 人審 Minimal Patch 是否只修合約違反 → 卡 3 放行。
- **驗收**:擋牌消失、綠色通過回來、diff 只有 `report.json` 一行。
- **保底**:JSON 改壞語法出現 Vite 全螢幕錯誤 → 改回原樣即恢復(這本身就是看錯誤訊息的練習)。

## 段 4 老師示範真送 + build 收尾(3:20–3:55)
- **老師示範**:用已設定的 `line-lab/.env` 跑 `LINE_REAL_SEND=1 … --flex --confirm`,全班看手機收到 Flex 通知。LINE 官方後台設定(Business ID/OA/token/targetId)只給進階組,見 `u11-c3-line-oa-lab.md` 附錄。
- **收尾**:`npm run build` → `git status` / `git diff` / `git diff -- web-lab/package.json`(必須無輸出)→ commit。
- **加做(進階組)**:Flex Message 版(`u11-c3-flex-message-lab.md`)、請 AI 擴充 Dashboard(`U3/PROMPT-CARD.md` 卡 4)。
- **驗收**:build 通過、diff 乾淨、commit 完成。

**C3 收束一句**:今天看到 `[mock]` 才算對,不是手機收到 LINE 才算對。做得出功能、修得好錯、還守得住人審 —— 你從「會問」變「會做」。

---

# C4 ｜ 自動化交付(0:00–4:00)

> 目標:**把 U3 做好的平台流程自動化**:CrewAI-style ops agent → GitHub Actions artifact → reviewer/build/commit → 固化成 `/ops-check` 與 `/ship-check`。
> MCP 只保留概念頁與老師示範,安裝細節退到〈附錄 C〉,不與主線爭時間。學生講義:`U4/STEP-01..03.md`。

## 段 1 ops agent:三個角色產出 report(0:15–1:05)
- 打開 `ops-agent-lab/role-cards.md`:data_checker / ops_decider / push_writer。
- 本機跑 `python ops-agent-lab/run_ops_check.py` 看 dry-run JSON。
- 跑 `python ops-agent-lab/run_ops_check.py --write-report` → `node line-lab/sendLineAlert.js --flex`。
- **驗收**:同一份 `report.json` 合約可以被 Dashboard 與 LINE Flex 腳本吃。

## 段 2 GitHub Actions:排程產 artifact,不真送(1:05–1:55)
- 打開 `.github/workflows/u11-ops-check.yml` 看四步:checkout → Python/Node → 產 report → 產 Flex payload → upload artifact。
- 有遠端的班級 push 後到 Actions 手動跑 `U11 Ops Agent Check`。
- **驗收**:artifact 內有 `report.json` 與 `line-flex-payload.json`;workflow 沒有 token,沒有真送 LINE。

## 段 3 reviewer + build/commit/push(1:55–2:45)
- **reviewer**:貼 `prompts/07-reviewer.md`。重點看 Platform Check / Contract Check / Package Check。reviewer 回報仍要對照 `git diff` 抽查。
- **操作**:`cd web-lab && npm run build && cd ..` → `git status` → `git diff` → `git add .` → `git commit`(可用 `/commit-msg`)→ 有遠端則 `git push`。
- **驗收**:reviewer PASS、build 通過、diff 只有預期變更、commit 訊息清楚。

## 段 4 把流程固定成 Skills / SOP + 自建 /ops-check /ship-check(2:45–3:25)
- **概念**:Skill = 給新人的工作守則,貼牆上,不用每次重講。放 `.claude/skills/<name>/SKILL.md`,`/name` 呼叫,也會自動觸發。
- **動手**:每人建立 `.claude/commands/ops-check.md` 與 `.claude/commands/ship-check.md`,打 `/ops-check`、`/ship-check` 各跑一次。
- **驗收**:`/ops-check` 檢查 ops/Actions/LINE mock,`/ship-check` 回四點且不改檔。

## 段 5 MCP 概念頁 + 老師示範(3:25–3:55)
- **概念(一頁)**:MCP = 幫 AI 接上外部工具的「安全的線」。裝之前先問:它**能讀什麼、能不能寫、會不會碰正式資料**。
- **老師示範**:用 Chrome DevTools MCP 開 `localhost`、看 console、截圖驗收 Dashboard。學生不裝;想裝的看〈附錄 C〉(安裝細節與逐類保底都在附錄)。
- **驗收**:學生說得出「權限三問」即可。

**C4 收束一句**:會用一次不稀奇,能讓 AI **每次照流程做**、還能**交接**,才是真本事。**你不是只會問 AI,你是會管理 AI 做事。**

---

# 附錄 A ｜ slash command 型錄

> **內建**:Claude Code 內建就有。**自建**:本課在 `.claude/commands/<name>.md` 建立(內文即 prompt,可用 `$ARGUMENTS`,frontmatter 填 `description`);Codex 亦支援自訂指令。
> 查 `/help` 看你這版所有可用指令。**注意**:Plan Mode 是「模式」(shift+tab 切),不是指令。

### A1 · plan(內建 · 模式)
- **用途**:讓 AI 只計畫、不改檔,先看它想改多少。
- **設定**:免安裝。**shift+tab** 循環到 Plan;部分版本可打 `/plan`。
- **固定操作**:切到 Plan mode → 貼需求 → 看計畫。
- **固定 prompt**:`prompts/02-plan-mode.md`。
- **預期結果**:AI 只回「會改哪個檔、哪一段、風險、驗收方式」,一個檔都沒動。
- **驗收**:`git status` 乾淨。
- **保底**:Windows shift+tab 跳過 Plan → 點面板底部模式字樣選 Plan。

### A2 · /status(內建)
- **用途**:看目前工作階段/設定狀態(與 `git status` 分工:一個看 AI 階段、一個看檔案改動)。
- **設定**:免安裝。
- **固定操作**:輸入框打 `/status`。
- **固定 prompt**:無(直接指令)。
- **預期結果**:顯示目前模型、模式、專案等狀態摘要。
- **驗收**:看得到狀態列。
- **保底**:看不懂就忽略,改看面板底部指示器 + `git status`。

### A3 · diff / 看 AI 改了什麼(內建 `/review` + 終端機 `git diff`)
- **用途**:改完後,先看它動了哪些檔、每行怎麼變。
- **設定**:`git diff` 免安裝;`/review` 內建;精緻版用自建 `/review-diff`(見 B5)。
- **固定操作**:終端機 `git diff`;或面板打 `/review`。
- **固定 prompt**:無(或用 `/review-diff`)。
- **預期結果**:綠加紅刪,只在預期範圍。
- **驗收**:沒有不相關檔案被動到。
- **保底**:`/review` 不存在此版 → 一律用 `git diff`。

### A4 · /debug(自建 → `.claude/commands/debug.md`)
- **用途**:遇到錯誤,先分析(不改)、同意後只改必要檔。
- **設定**:本課已建於 starter;內容即 `prompts/04-debug.md`。
- **固定操作**:打 `/debug` 後貼上錯誤訊息(或 `/debug <錯誤描述>`)。
- **固定 prompt**:見 `.claude/commands/debug.md`。
- **預期結果**:先給「可能原因/相關檔/如何確認/建議修法」,等你同意才改。
- **驗收**:修完後 Dashboard 擋牌消失、`node line-lab/sendLineAlert.js --flex` 恢復 mock 成功。
- **保底**:指令沒載入 → 直接貼 `prompts/04-debug.md` 全文。

### A5 · /clear、/compact(內建)
- **用途**:`/clear` 清空對話從頭開始(省 token、換任務時用);`/compact` 壓縮長對話成摘要、保留重點。
- **設定**:免安裝。
- **固定操作**:換任務前 `/clear`;對話太長、AI 開始忘記時 `/compact`。
- **固定 prompt**:無。
- **預期結果**:`/clear` 後上下文清空;`/compact` 後對話變短但重點還在。
- **驗收**:AI 不再被舊對話干擾。
- **保底**:`/compact` 後仍亂 → 直接 `/clear` 重新開始、重貼必要 context。

### A6 · commit message(自建 → `.claude/commands/commit-msg.md`)
- **用途**:根據 `git diff` 產生一句清楚的 commit 訊息(講改了什麼、為什麼)。
- **設定**:本課已建於 starter。
- **固定操作**:改完、`git add` 後打 `/commit-msg`。
- **固定 prompt**:見 `.claude/commands/commit-msg.md`(讀 staged diff、產出一行 summary + 選填說明)。
- **預期結果**:一句可直接用的 commit 訊息。
- **驗收**:訊息看得出「改了什麼」。
- **保底**:指令沒載入 → 自己寫「動詞 + 對象」如「修正 risk_level 輸出格式」。

---

# 附錄 B ｜ Skills 型錄(5 支,`.claude/skills/<name>/SKILL.md`)

> **共同**:設定 = 放在 `ai-project-foundation-kit/.claude/skills/<name>/SKILL.md`(clone 即帶);呼叫 = 打 `/<name>`,相關情境也會自動觸發;`/help` 或 `/skills` 可看到。**共同保底**:Skill 沒觸發 → 直接貼對應 `prompts/*.md` 全文。

| # | Skill | 用途 | 固定操作 | 固定 prompt 來源 | 預期結果 | 驗收 |
|---|---|---|---|---|---|---|
| B1 | `beginner-ai-project-workflow` | 新手接手專案的總守則(先讀→plan→最小改→回報→Git) | 接手任何專案時 `/beginner-ai-project-workflow` | 既有 `skill/…md` 遷入 | AI 照「讀→計畫→小改→驗收」走 | 它先讀、不亂改 |
| B2 | `data-check-fixed-output` | 看資料一律輸出固定四欄 | 給資料時 `/data-check-fixed-output` | `prompts/05-data-check.md` | 四欄 + risk_level 受限 + 不可採用標記 | 格式每次一致 |
| B3 | `debug-react` | 用 ReAct 修錯:先想、再做、再看 | 遇錯 `/debug-react` | `prompts/04-debug.md` | 先分析後才改、只改必要檔 | 修好且 diff 乾淨 |
| B4 | `git-verify` | 收尾三件套 status/diff/commit | 改完 `/git-verify` | `prompts/06` Git 段 | 引導 status→diff→commit | diff 只有預期變更 |
| B5 | `review-diff` | 專門檢視 diff、抓越界修改 | 改完 `/review-diff` | 新寫(讀 diff、列出動到的檔與風險) | 逐檔列出改了什麼、有無越界 | 抓得出不相關改動 |

---

# 附錄 C ｜ MCP 型錄(3 類)

> **共同概念**:MCP 讓 AI 在**你授權範圍內**自己去外部工具讀資料。裝前先問:能讀什麼、能不能寫、會不會碰正式資料。裝法用 Claude Code CLI `claude mcp add`,或專案層 `.mcp.json`(本課已附)。裝完打 `/mcp` 檢查。**共同保底**:連不上 → `/mcp` 看狀態、重開 VS Code;`npx` 失敗 → 確認 Node 18+;仍不行 → 退回終端機/手動,不擋進度。

### C1 · Chrome DevTools MCP(驗收 web-lab)
- **用途**:讓 AI 開真的 Chrome、看 console/network/DOM、截圖 —— 拿來驗收 `web-lab`。
- **安裝**:`claude mcp add chrome-devtools -- npx chrome-devtools-mcp@latest`(需 Node + Chrome)。或 `.mcp.json` 加一條。
- **固定操作**:先 `npm run dev` 跑起 web-lab → 請 AI「打開 localhost、看 console 有無錯誤、截一張圖」。
- **固定 prompt**:`請用 Chrome DevTools 開 http://localhost:5173,列出 console 有沒有錯誤,並截一張首頁的圖。`
- **預期結果**:AI 回報 console 狀況 + 一張截圖。
- **驗收**:`/mcp` 列出 chrome-devtools;真的拿到截圖。
- **保底**:連不上 → 自己在瀏覽器按 F12 看 console。

### C2 · Context7 MCP(查最新文件)
- **用途**:把套件**最新官方文件**餵給 AI,避免它用過時 API。
- **安裝**:`claude mcp add context7 -- npx -y @upstash/context7-mcp`(選填 `--api-key`,免費金鑰於 context7.com/dashboard;或 remote:`claude mcp add --transport http context7 https://mcp.context7.com/mcp`)。
- **固定操作**:問套件用法時,句尾加 `use context7`。
- **固定 prompt**:`用 vite 設定 dev server 埠號,請參考最新官方文件。use context7`
- **預期結果**:AI 依最新文件回答,而非記憶。
- **驗收**:`/mcp` 列出 context7;答案附上文件出處。
- **保底**:沒金鑰限流 → 改用官方網站,或改用 `/model` 換更強模型。

### C3 · Codebase Memory MCP(讓 AI 記住整個專案)
- **用途**:把程式碼庫索引成本地知識圖譜(SQLite),AI 能查**架構總覽、呼叫關係、死碼**,不用一檔一檔翻。全本地執行、**免 API key**。
- **安裝**(官方腳本會自動偵測並設定 Claude Code 等 agent):
  - Windows PowerShell:`Invoke-WebRequest -Uri https://raw.githubusercontent.com/DeusData/codebase-memory-mcp/main/install.ps1 -OutFile install.ps1` → `.\install.ps1`
  - macOS/Linux:`curl -fsSL https://raw.githubusercontent.com/DeusData/codebase-memory-mcp/main/install.sh | bash`
  - repo:https://github.com/DeusData/codebase-memory-mcp
- **固定操作**:先請 AI 用 `index_repository` 索引專案 → 再問架構問題。
- **固定 prompt**:`請先索引這個專案,然後告訴我:整體架構長怎樣?Dashboard.jsx 被誰使用?`
- **預期結果**:AI 用圖譜回答架構與檔案關係,不再逐檔猜。
- **驗收**:`/mcp` 列出 codebase-memory;架構問題答得出檔案關係。
- **保底**:裝不起來 → 用 `/init` 讓 AI 讀專案,或直接看 README;不擋進度。索引資料存在本機 `~/.cache/codebase-memory-mcp/`,不會上傳。

---

# 通用保底(任何一步都適用)

| 狀況 | B 方案 |
|---|---|
| AI 亂改一堆 | `git status` 看範圍 → `git restore <檔>` 或 `git checkout -- <檔>` 還原 → 加更嚴格護欄重下 |
| `npm install` / `npx` 失敗 | 確認 Node 18+(`node -v`);網路;仍不行看老師畫面,流程不中斷 |
| Python 跑不動 | 用線上 Python 或看老師示範,重點在 debug 流程 |
| Claude Code / Codex 連不上 | 重開 VS Code、重新登入;仍不行改用網頁版整理想法,動手部分看示範 |
| MCP 連不上 | `/mcp` 看狀態、重開;退回終端機/手動 |
| shift+tab 跳過 Plan(Windows) | 點面板底部模式字樣選 Plan,或打 `/plan` |
| 全班進度落差大 | 老師每 10–15 分對齊一次;落後者用保底方案跟上,不卡整班 |

> 相關檔案:固定 prompt 在 `prompts/`、可執行 Skill/指令在 `.claude/`、MCP 設定在 `.mcp.json`、資料檢查在 `data-lab/`。四堂簡報在 `slides/slides/u11-c1..c4`。
